{
  "fixtures": [
    {
      "match": { "userMessage": "run|execute|calculate|compute" },
      "response": {
        "toolCalls": [
          {
            "name": "bash",
            "arguments": "{\"command\": \"echo 'Hello from the sandbox worker!'\"}"
          }
        ]
      }
    },
    {
      "match": { "toolName": "bash" },
      "response": {
        "content": "The tool executed successfully. Output: Hello from the sandbox worker!"
      }
    },
    {
      "match": { "model": ".*" },
      "response": { "content": "Mock response from llmock." }
    },
    {
      "match": {},
      "response": { "content": "Mock response from llmock (fallback)." }
    }
  ]
}
