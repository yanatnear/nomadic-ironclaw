{
  "fixtures": [
    {
      "match": { "userMessage": "echotest" },
      "response": {
        "toolCalls": [
          {
            "name": "echo",
            "arguments": "{\"message\": \"Hello from llmock tool call!\"}"
          }
        ]
      }
    },
    {
      "match": { "userMessage": "jobtest" },
      "response": {
        "toolCalls": [
          {
            "name": "create_job",
            "arguments": "{\"title\": \"llmock sandbox test\", \"description\": \"sandbox worker test\"}"
          }
        ]
      }
    },
    {
      "match": { "userMessage": "sandbox worker test" },
      "response": {
        "content": "I have completed the task. The sandbox worker test is complete."
      }
    },
    {
      "match": {},
      "response": { "content": "The task is complete." }
    }
  ]
}
