{
  "fixtures": [
    {
      "match": { "toolName": "echo" },
      "response": { "content": "Echo tool result received. Test complete." }
    },
    {
      "match": { "userMessage": "echo" },
      "response": {
        "toolCalls": [
          {
            "name": "echo",
            "arguments": "{\"message\": \"Hello from llmock tool call!\"}"
          }
        ]
      }
    },
    {
      "match": { "toolName": "create_job" },
      "response": { "content": "Job created successfully. Test complete." }
    },
    {
      "match": { "userMessage": "run" },
      "response": {
        "toolCalls": [
          {
            "name": "create_job",
            "arguments": "{\"title\": \"llmock sandbox test\", \"description\": \"Run echo in a sandbox worker to verify tool dispatch.\"}"
          }
        ]
      }
    },
    {
      "match": {},
      "response": { "content": "Mock response from llmock (fallback)." }
    }
  ]
}
